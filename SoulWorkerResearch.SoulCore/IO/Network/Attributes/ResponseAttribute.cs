using SoulWorkerResearch.SoulCore.IO.Network.Commands;

namespace SoulWorkerResearch.SoulCore.IO.Network.Attributes
{
    public sealed class ResponseAttribute : BasePacketAttribute
    {
        internal ResponseAttribute(CategoryCommand category, SystemCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, LoginCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, CharacterCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, WorldCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, MoveCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, SkillCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ChatCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ItemCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ShopCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, TradeCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, MazeCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, PartyCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ItemUpgradeCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, DropCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, QuestCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, OptionCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, MonsterCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ItemSetupCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, FriendCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, PostCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, SoulMetryCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, LeagueCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, GestureCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, DailyMissionCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, VaccumCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, MyRoomCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, HelperCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, InfiniteTowerCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, BoosterCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, EventCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ExchangeCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, RankingCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, SocialItemCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ForceCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, WorldModeCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, WeeklyMissionCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ModeMazeCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, RestartCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ToolCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ChannelCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerUserCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerPartyCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerFriendCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerLeagueCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, MonitorCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, GmAgentCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerForceCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerWorldModeCommand command) : base(category, (byte)command)
        {
        }

        internal ResponseAttribute(CategoryCommand category, ServerModeMazeCommand command) : base(category, (byte)command)
        {
        }
    }
}
