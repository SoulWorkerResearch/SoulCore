using SoulWorkerResearch.SoulCore.IO.Network.Commands;

namespace SoulWorkerResearch.SoulCore.IO.Network.Attributes
{
    public sealed class RequestAttribute : BasePacketAttribute
    {
        internal RequestAttribute(CategoryCommand category, SystemCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, LoginCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, CharacterCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, WorldCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, MoveCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, SkillCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ChatCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ItemCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ShopCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, TradeCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, MazeCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, PartyCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ItemUpgradeCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, DropCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, QuestCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, OptionCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, MonsterCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ItemSetupCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, FriendCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, PostCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, SoulMetryCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, LeagueCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, GestureCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, DailyMissionCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, VaccumCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, MyRoomCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, HelperCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, InfiniteTowerCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, BoosterCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, EventCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ExchangeCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, RankingCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, SocialItemCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ForceCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, WorldModeCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, WeeklyMissionCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ModeMazeCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, RestartCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ToolCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ChannelCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerUserCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerPartyCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerFriendCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerLeagueCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, MonitorCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, GmAgentCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerForceCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerWorldModeCommand command) : base(category, (byte)command)
        {
        }

        internal RequestAttribute(CategoryCommand category, ServerModeMazeCommand command) : base(category, (byte)command)
        {
        }
    }
}
